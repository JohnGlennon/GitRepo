@model IEnumerable<PB_MVC.Models.AdminUserViewModel>

@{
  ViewBag.Title = "Index";
}

<h1>Dit is de adminpagina!!!</h1>

<h2>Index</h2>


<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Email)
        </th>
    </tr>

    @foreach (var item in Model)
    {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Email)
        </td>
    </tr>
    }
</table>